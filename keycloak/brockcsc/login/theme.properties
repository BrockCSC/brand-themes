parent=keycloak.v2
import=common/keycloak

# styles replaces the parent's list rather than appending, so css/styles.css
# (resolved from keycloak.v2) has to be repeated before ours.
styles=css/styles.css css/brockcsc.css

meta=viewport==width=device-width,initial-scale=1
